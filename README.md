# MegaSort Final Project

This repository contains the user's uploaded final MegaSort project as a deployment payload.

Vercel automatically reconstructs the complete source from the .payload parts during the build, then runs the real Vite production build. Supabase login, guest mode and backup features are included.

The private .env file is intentionally not committed. The Supabase browser client uses the project's public publishable configuration.
